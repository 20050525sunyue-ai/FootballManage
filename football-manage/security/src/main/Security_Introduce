========================
Spring Security + JWT 登录完整后端流程
========================

【你自己写的】
LoginController.login()   也就是/user/login这个接口

        ↓ 调用

【你自己写的】   调用登录service中的login
LoginServiceImpl.login()

        ↓ 调用（外部调用）     这里调用AuthenticationManager.authenticate(Token)
                            参数：new UsernamePasswordAuthenticationToken(loginDTO.getUsername(), loginDTO.getPassword())
【需要注册 Bean】                               需要将AuthenticationManager注册为Bean，才可调用
AuthenticationManager.authenticate()

AuthenticationManager：
是接口

真正注入进来的：

【Spring Security 内部】
ProviderManager         是AuthenticationManager的实现类， 它只负责：“安排谁去认证”

====================================================

ProviderManager.authenticate()

        ↓ 内部调用

【Spring Security 内部】
DaoAuthenticationProvider.authenticate()

====================================================

DaoAuthenticationProvider          这个是真正干活的，去调用我们在UserDetailsService的实现类中写的loadUserByUsername()，
                                                  返回值UserDetails，

        ↓ 内部调用

【你自己写的】
UserDetailsServiceImpl.loadUserByUsername()        这个方法其实就是通过名字调用我们自己写的mapper去查询用户信息，获取密码，
                                                    然后通过内部PasswordEncoder.matches()校验密码

====================================================

UserDetailsServiceImpl.loadUserByUsername()

        ↓ 调用

【你自己写的】
loginMapper.selectUserInfo(username)

        ↓ 查询数据库

mysql

        ↓ 返回

SysUser

====================================================

UserDetailsServiceImpl                  loadUserByUsername返回值是 UserDetails类型的

        ↓ 返回

【Spring Security 内部对象】
UserDetails

比如：

new User(
    username,
    password,
    authorities
)

====================================================

DaoAuthenticationProvider

        ↓ 内部调用

【需要注册 Bean】
PasswordEncoder.matches()           返回值boolean类型的

PasswordEncoder：
接口

真正使用：

BCryptPasswordEncoder

====================================================

PasswordEncoder.matches(
    前端明文密码,
    数据库 BCrypt 密文
)

        ↓

密码正确：

返回 true

密码错误：

抛 BadCredentialsException

====================================================

DaoAuthenticationProvider

        ↓ 内部创建

【Spring Security 内部对象】
Authentication

此时：

authenticated=true

====================================================

DaoAuthenticationProvider

        ↓ 返回给内部

ProviderManager

        ↓ 返回给外部

authenticationManager.authenticate()

====================================================

回到：

【你自己写的】
LoginServiceImpl.login()

====================================================

Authentication authenticate
=
authenticationManager.authenticate(...)

能执行到这里：

说明：

登录成功

====================================================

然后：

【你自己写的业务逻辑】

1. 查询用户信息
2. 生成 JWT
3. 封装 LoginVo
4. 返回前端
